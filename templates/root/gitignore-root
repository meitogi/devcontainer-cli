# DevContainer (v3) — root-scope

# Claude — local runtime artefacts
.claude/*
!.claude/rules/
!.claude/rules/**
!.claude/settings.local.json.example

# VS Code — bind-mount via docker-compose, single tracked file
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# devcontainer CLI — installed by `npm install` from package.json
node_modules/

# Build / OS
.env.dev
.DS_Store
